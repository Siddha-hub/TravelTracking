specVersion: "3.0"
metadata:
  name: budgetmanagerui
type: application
builder:
  cachebuster:
    signatureType: hash
  resources:
    excludes:
      - "/test/**"
      - "/localService/**"
  customTasks:
    - name: ui5-task-minifier
      afterTask: minify
    - name: ui5-task-zipper
      afterTask: generateCachebusterInfo
      configuration:
        archiveName: budgetmanagerui
        additionalFiles:
          - xs-app.json
